VERSION=@VERSION@

prefix=
bindir=
libdir=
sharedir=
INSTALL=
MAKE=

targets:
	sed -e "s:@VERSION@:$(VERSION):g" \
		-e "s:@LIBDIR@:$(libdir):g" \
		-e "s:@SHAREDIR@:$(sharedir):g" \
		lblmail.in > lblmail

install:
	$(INSTALL) -m 755 lblmail $(DESTDIR)$(bindir)/lblmail

clean:
	rm -f lblmail
